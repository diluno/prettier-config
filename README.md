# `@diluno/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @diluno/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@diluno/prettier-config"
}
```
